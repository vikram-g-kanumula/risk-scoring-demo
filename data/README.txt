# This directory holds generated data files (created by setup.py).
# Files listed here are excluded from git via .gitignore.
#
# After running `python setup.py`, you will find:
#   homeowners_data.csv   — 100,000 synthetic policies (full dataset)
#   train_data.csv        — 80,000 training policies
#   test_data.csv         — 20,000 held-out test policies
